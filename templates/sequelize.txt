import Env from '@ioc:Adonis/Core/Env';
import { SequelizeConfig, Dialect } from '@ioc:Adonis/Sequelize';

const sequelizeConfig: SequelizeConfig = {
  /*
  |--------------------------------------------------------------------------
  | Dialect
  |--------------------------------------------------------------------------
  | TODO: [TRANSLATE] перевести на англ.
  | Указать тип движка
  | Доступные - mysql, postgres, sqlite, mariadb
  |
  */
  dialect: Env.get('DB_DIALECT', 'sqlite' as Dialect),

  /*
  |--------------------------------------------------------------------------
  | Host
  |--------------------------------------------------------------------------
  | TODO: [TRANSLATE] перевести на англ.
  | Указать хост, на котором база данных
  |
  */
  host: Env.get('DB_HOST', 'localhost'),

  /*
  |--------------------------------------------------------------------------
  | Port
  |--------------------------------------------------------------------------
  | TODO: [TRANSLATE] перевести на англ.
  | Указать порт, на котором база данных
  |
  */
  port: Env.get('DB_PORT', 5432),

  /*
  |--------------------------------------------------------------------------
  | Database name
  |--------------------------------------------------------------------------
  | TODO: [TRANSLATE] перевести на англ.
  | Наименование базы данных
  |
  */
  database: Env.get('DB_NAME', 'database_name'),

  /*
  |--------------------------------------------------------------------------
  | Database username
  |--------------------------------------------------------------------------
  | TODO: [TRANSLATE] перевести на англ.
  | Пользователь базы данных
  |
  */
  username: Env.get('DB_USER', 'user'),

  /*
  |--------------------------------------------------------------------------
  | Database password
  |--------------------------------------------------------------------------
  | TODO: [TRANSLATE] перевести на англ.
  | Пароль базы данных
  |
  */
  password: Env.get('DB_PASSWORD', 'secret'),

  /*
  |--------------------------------------------------------------------------
  | Pool settings
  |--------------------------------------------------------------------------
  | TODO: [TRANSLATE] перевести на англ.
  | Настройки соединения
  |
  */
  pool: {
    min: 0,
    max: 5,
    idle: 10000
  }
};

export default sequelizeConfig;
